{
  "name": "convex-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Throwaway feasibility spike for @baas/adapter-convex. NOT a workspace package.",
  "scripts": {
    "codegen": "convex codegen",
    "convex:once": "convex dev --once",
    "convex:dev": "convex dev",
    "test": "vitest run",
    "spike": "node --env-file=.env.local --import tsx spike.ts"
  },
  "dependencies": {
    "convex": "^1"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5",
    "convex-test": "latest",
    "tsx": "^4",
    "typescript": "^5.7",
    "vitest": "^3"
  }
}

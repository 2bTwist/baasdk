# Env (Convex CLI writes .env.local; it also holds the Supabase dev keys)
.env.local
.env.keys
.env.*.local

# Convex codegen, regenerated by `npx convex dev`
convex/_generated/

# Supabase CLI local state
supabase/.branches
supabase/.temp
